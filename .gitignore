# Xcode user data (per-user, do not commit)
**/*.xcuserdatad/
**/*.xcuserdata/
**/xcuserdata/

# Workspace user data
**/*.xcworkspace/xcuserdata/

# Keep shared schemes so others can build/run
# (do NOT ignore xcshareddata)

# macOS metadata
.DS_Store

# Secrets / environment-specific files
**/GoogleService-Info.plist

# If your project relies on an Info.plist file, keep it tracked.
# Uncomment the next line ONLY if you intentionally generate Info at build time.
**/Info.plist
